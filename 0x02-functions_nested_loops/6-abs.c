#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer
 * @i: parameter
 *
 * Return: absolute interger
 */
int _abs(int i)
{
	if (i < 0)

{
	i = -i;
}
	return (i);
}
