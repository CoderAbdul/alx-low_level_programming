#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @x: first integer
 * @y: second integer
 * Return: returns 0
 */
int mul(int x, int y)
{
	int result;

	result = (x * y);
	return (result);
}
