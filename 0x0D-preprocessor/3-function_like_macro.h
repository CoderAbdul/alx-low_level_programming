#ifndef _FUNCTION_LIKE_MACRO_H_
#define _FUNCTION_LIKE_MACRO_H_
/**
 * file - computes the absolute value of a number
 * @x: value
 * Return: an int
 */

#define ABS(x) ((x) < 0 ? (x) * -1 : (x))

#endif
