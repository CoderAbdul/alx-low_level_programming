#include "main.h"
#include <stdio.h>

/**
 * main -Causes an infinite loop
 *
 * Return: returns 0
 */

int main(void)
{
	int x;

	printf("Infinite loop incoming :(\n");

	x = 0;

	/**
	 * while (x < 10)
	 * }
	 * putchar(x);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
